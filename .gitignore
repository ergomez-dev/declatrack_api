node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Seed scripts contienen credenciales de arranque — fuera de control de versiones
prisma/seed/

# Herramientas locales / plugins (Graphify, config de Claude Code)
graphify-out/
.claude/
CLAUDE.md
